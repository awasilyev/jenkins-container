---
- name: Test jenkins service   
  hosts: localhost
  connection: local
  gather_facts: no
  vars:
    host_ip: 127.0.0.1
  tasks:
    - name: Wait for service to be available
      wait_for:
        host: "{{ host_ip }}"
        port: 8080
        delay: 30
        timeout: 120 
